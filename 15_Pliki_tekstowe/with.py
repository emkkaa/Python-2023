with open('plik.txt', 'a') as f: #with odnosi sie do kontekst manager python pilnuje by go otworzyc i zamknac (with = try z finally w ktorym jest close)
    f.write("Hello\n") #jak w write_check to wszystko tylko w jednym slowku with
